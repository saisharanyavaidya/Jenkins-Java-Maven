pipeline {
    label {
        agent 'AGENT-1'
    }

    stages {
        stage('Checkout scm'){
            steps{
                checkout scm
                echo "success"
            }
        }
    }
}